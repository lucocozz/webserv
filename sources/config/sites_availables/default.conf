	
server {

	root /var/www/blabla;

	server_name _;

	location /cgi-php/{
		cgi_extension 	.php;
		cgi_binary		php-cgi;
	}

	# location yolo {

	# }

	# location /blog/ {
	# 	index index.html;
	# }
}

server {
	listen 8081;
	server_name yolo;

	root /;

	index index.html;

	error_log off;
	access_log /dev/stdout;

	location / {
		# error_page 404 /;
		# client_max_body_size 100M;
		# autoindex on;
	}
}
